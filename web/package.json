{
  "name": "pylon-synth-web",
  "version": "1.0.0",
  "description": "three.js control surface for pylon-synth; dispatches MIDI CC over the Web MIDI API.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler pages deploy ./dist --project-name pylon-synth"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "three": "0.160.0"
  },
  "devDependencies": {
    "wrangler": "^4.95.0"
  }
}
